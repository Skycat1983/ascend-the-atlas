// we might want to shift avaialbility of country, region or modifier from available to unavailable
export const shiftDataAvailability = (
  state: any,
  dispatch: any
): Promise<void> => {
  return new Promise((resolve) => {
    // bla bla bla

    resolve();
  });
};
