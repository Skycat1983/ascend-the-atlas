import React from "react";
import Form from "../Components/MyForm";
import useForm from "../customHooks/useForm";

type Props = {};

const Signup = (props: Props) => {
  // const { state, errors, handleChange, handleSubmit, validate }
  return (
    <div>
      Signup
      <Form></Form>
    </div>
  );
};

export default Signup;
